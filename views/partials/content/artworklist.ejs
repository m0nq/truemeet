<article class="artworklist">
  <h1>Artwork on display</h1>
  <p>While you attend the conference, head over to ur gallery where you can see more work from our some of our speakers.</p>
  <div class="pixgrid">
    <% if (artwork.length > 0) { %>
      <% for (var i = 0; i < artwork.length; i++) { %>
        <img src="/images/artwork/<%= artwork[i] %>" alt="Artwork <%= i %>"/>
      <% } %>
    <% } %>
  </div>
</article>
